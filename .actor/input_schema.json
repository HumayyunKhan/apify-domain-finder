{
    "title": "Domain Email Finder by Scalelist",
    "description": "Provide a list of domains to search. You are charged per email found.",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "domains": {
            "title": "Domain List",
            "type": "array",
            "description": "List of domains to search. Each entry requires a domain, with an optional company_name.",
            "editor": "schemaBased",
            "items": {
                "type": "object",
                "properties": {
                    "domain": {
                        "title": "Domain",
                        "type": "string",
                        "description": "The company domain to search, e.g. acmecorp.com (required)",
                        "editor": "textfield"
                    },
                    "company_name": {
                        "title": "Company Name",
                        "type": "string",
                        "description": "Company name — optional, helps improve matching accuracy",
                        "editor": "textfield"
                    }
                },
                "required": [
                    "domain"
                ]
            },
            "prefill": [
                {
                    "domain": "thescalelab.com",
                    "company_name": "The Scalelab"
                },
                {
                    "domain": "scalelist.com",
                    "company_name": "Scalelist"
                }
            ]
        },
        "limit": {
            "title": "Email Limit per Domain",
            "type": "integer",
            "description": "Maximum number of emails to return for each domain. Defaults to 10.",
            "editor": "number",
            "minimum": 1,
            "maximum": 100,
            "default": 10
        }
    },
    "required": [
        "domains"
    ]
}